services:
  web:
    image: nginx:alpine
    ports:
      - "3000:80"
    user: "0:0"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.base44.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx.main.base44.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
